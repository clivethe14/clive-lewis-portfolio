# Drop `resume.pdf` here

The site's nav links to `/resume.pdf`. Copy the real PDF into this directory as
`resume.pdf` (exact filename) before deploying, then delete this file.

Anything in `public/` is copied verbatim into `out/` by `next build`.
